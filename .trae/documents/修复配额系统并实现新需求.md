## 实现计划

### 1. AI对话组件改造
- **需求**：将AI对话改为只读模式，只回答问题不修改图数据，确保两个页面解耦
- **实现方案**：
  - 修改AI对话组件，添加`isReadOnly`配置选项
  - 确保AI对话只读取图数据，不修改图数据
  - 实现页面标识机制，区分Pagerank页面和树算法页面
  - 修改相关组件代码，确保两个页面的数据隔离

### 2. 配额系统优化
- **需求**：将默认配额改为10次，root用户不限额
- **实现方案**：
  - 修改`app/api/chat/proxy/route.ts`中创建学生记录的默认配额值
  - 添加root用户特殊处理逻辑，跳过配额验证
  - 修复配额检查逻辑，确保所有请求类型都受限制
  - 确保使用计数正确更新

### 3. 树可视化滚动条优化
- **需求**：确保树可视化区能够正确显示滚动条
- **实现方案**：
  - 检查并优化树可视化组件的样式
  - 确保父容器的overflow属性设置正确
  - 测试大量节点情况下的滚动效果
  - 调整树节点的间距和大小，优化显示效果

### 4. 测试与验证
- 验证AI对话只读模式功能
- 验证两个页面的数据隔离
- 验证配额系统的正确性
- 验证root用户无配额限制
- 验证树可视化滚动条功能

### 5. 代码优化
- 确保代码的可读性和可维护性
- 添加必要的注释和文档
- 优化性能，确保系统流畅运行
- 保持代码的一致性和规范性

## 预期效果
- AI对话只回答问题，不修改图数据
- 两个页面的数据完全隔离，互不影响
- 普通用户配额限制为10次，root用户无限制
- 树可视化区域能够正确显示滚动条
- 系统性能良好，响应迅速